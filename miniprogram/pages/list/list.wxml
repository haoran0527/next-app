<view class="container">
  <view class="filter-section">
    <view class="filter-header">
      <text class="filter-title">筛选条件</text>
      <view class="filter-reset" bindtap="onResetFilter">
        <text class="reset-text">重置</text>
      </view>
    </view>
    
    <view class="filter-content">
      <view class="filter-row">
        <picker 
          mode="selector" 
          range="{{filterTypes}}" 
          bindchange="onFilterTypeChange"
          value="{{filterType === 'ALL' ? 0 : filterType === 'INCOME' ? 1 : 2}}"
        >
          <view class="filter-item">
            <view class="filter-left">
              <text class="filter-icon">📊</text>
              <text class="filter-label">交易类型</text>
            </view>
            <view class="filter-right">
              <text class="filter-value {{filterType !== 'ALL' ? 'active' : ''}}">{{filterTypes[filterType === 'ALL' ? 0 : filterType === 'INCOME' ? 1 : 2]}}</text>
              <text class="filter-arrow">›</text>
            </view>
          </view>
        </picker>
      </view>

      <view class="filter-divider"></view>

      <view class="filter-row">
        <picker mode="date" value="{{startDate}}" bindchange="onStartDateChange">
          <view class="filter-item">
            <view class="filter-left">
              <text class="filter-icon">📅</text>
              <text class="filter-label">开始日期</text>
            </view>
            <view class="filter-right">
              <text class="filter-value {{startDate ? 'active' : ''}}">{{startDate || '不限'}}</text>
              <text class="filter-arrow">›</text>
            </view>
          </view>
        </picker>
      </view>

      <view class="filter-divider"></view>

      <view class="filter-row">
        <picker mode="date" value="{{endDate}}" bindchange="onEndDateChange">
          <view class="filter-item">
            <view class="filter-left">
              <text class="filter-icon">📅</text>
              <text class="filter-label">结束日期</text>
            </view>
            <view class="filter-right">
              <text class="filter-value {{endDate ? 'active' : ''}}">{{endDate || '不限'}}</text>
              <text class="filter-arrow">›</text>
            </view>
          </view>
        </picker>
      </view>
    </view>
  </view>

  <view class="transaction-list">
    <view wx:if="{{transactions.length === 0 && !loading}}" class="empty">
      <text>暂无交易记录</text>
    </view>

    <view
      wx:for="{{transactions}}"
      wx:key="id"
      class="transaction-item"
      bindtap="goToDetail"
      data-id="{{item.id}}"
    >
      <view class="transaction-main">
        <view class="transaction-info">
          <view class="transaction-category">{{item.category}}</view>
          <view class="transaction-description" wx:if="{{item.description}}">{{item.description}}</view>
        </view>
        <view class="transaction-right">
          <view class="transaction-amount {{item.type === 'INCOME' ? 'income' : 'expense'}}">
            {{item.type === 'INCOME' ? '+' : '-'}}¥{{item.amount}}
          </view>
          <view class="transaction-time">
            <text class="time-icon">📅</text>
            <text>{{item.formattedTime}}</text>
          </view>
        </view>
      </view>
    </view>

    <view wx:if="{{loading}}" class="loading">
      <text>加载中...</text>
    </view>

    <view wx:if="{{!hasMore && transactions.length > 0}}" class="no-more">
      <text>没有更多了</text>
    </view>
  </view>
</view>
